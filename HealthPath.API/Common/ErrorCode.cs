namespace HealthPath.API.Common;

public enum ErrorCode
{
    // Auth
    EMAIL_TAKEN = 1001,
    INVALID_CREDENTIALS = 1002,
    UNAUTHORIZED = 1003,
    FORBIDDEN = 1004,
    INVALID_OTP = 1005,
    OTP_EXPIRED = 1006,
    USER_NOT_FOUND = 1007,
    EMAIL_NOT_VERIFIED = 1008,
    EXTERNAL_ACCOUNT_ALREADY_LINKED = 1009,
    EXTERNAL_ACCOUNT_PROVIDER_INVALID = 1010,

    // Routine
    ROUTINE_NOT_FOUND = 2001,
    PREMIUM_REQUIRED = 2002,
    FORBIDDEN_SYSTEM_ROUTINE = 2003,
    CATEGORY_INVALID = 2004,

    // UserRoutine / State machine
    USER_ROUTINE_NOT_FOUND = 3001,
    INVALID_STATE_TRANSITION = 3002,
    INSUFFICIENT_DURATION = 3003,
    ROUTINE_ALREADY_SCHEDULED = 3004,

    // Notification
    NOTIFICATION_NOT_FOUND = 4001,
    DEVICE_TOKEN_EXISTS = 4002,

    // File Upload
    FILE_TOO_LARGE = 5001,
    FILE_TYPE_NOT_ALLOWED = 5002,
    FILE_UPLOAD_FAILED = 5003,

    // Audio
    AUDIO_TRACK_NOT_FOUND = 6001,
    AUDIO_CATEGORY_INVALID = 6002,
    AUDIO_ALREADY_FAVORITED = 6003,
    AUDIO_NOT_FAVORITED = 6004,
    AUDIO_CATEGORY_NOT_FOUND = 6005,
    AUDIO_CATEGORY_NAME_TAKEN = 6006,
    AUDIO_CATEGORY_IN_USE = 6007,

    // Subscription / IAP
    SUBSCRIPTION_PLAN_NOT_FOUND = 7001,
    IAP_VERIFICATION_FAILED = 7002,
    SUBSCRIPTION_INACTIVE = 7003,
    TRANSACTION_NOT_FOUND = 7004,

    // Roles & Permissions
    ROLE_NOT_FOUND = 8001,

    // General
    VALIDATION_ERROR = 9001,
    INTERNAL_ERROR = 9999
}
